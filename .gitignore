# Secrets — never commit real Azure credentials
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Screenshot tooling is dev-only (playwright + chromium are large)
# but the generated images ARE committed — the README links to them.
